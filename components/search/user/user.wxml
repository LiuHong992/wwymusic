<!-- 内容层 -->
<view class="user-contents m-t-10" id="userconts" wx:if="{{$.afterSearch.length>0}}">
    <scroll-view class="scroll-view-container" scroll-y="{{true}}" bindscrolltolower="toBottom">
        <userModel users="{{$.afterSearch}}"></userModel>
    </scroll-view>
</view>